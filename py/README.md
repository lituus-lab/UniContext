# UniContext Python binding

This package is a Cython extension over the stable C ABI: the library version, the
ABI generation, and the token-budget bounds a caller needs before asking for a
context packet. The native library travels inside the wheel.
